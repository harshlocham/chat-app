{
    "name": "@chat/services",
    "version": "1.1.0",
    "scripts": {
        "build": "tsc -p tsconfig.build.json",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@chat/types": "*",
        "zod": "^4.0.5"
    }
}