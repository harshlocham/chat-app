'use client'
//import type { Metadata } from "next";
import "./globals.css";
import { ThemeProvider } from "@/providers/theme-provider";
import { SessionProvider } from "next-auth/react";
import { Toaster } from "@/components/ui/sonner"
import { UserProvider } from "@/context/UserContext";
import { SocketProvider } from "@/providers/socket-provider";


// export const metadata: Metadata = {
//   title: "Create Next App",
//   description: "Generated by create next app",
/**
 * Wraps the application children with global providers and renders the root HTML structure.
 *
 * @param children - Application content to render inside the theme, session, user, and socket providers
 * @returns The root HTML element containing the provider-wrapped application and a toast container
 */


export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
      //className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <ThemeProvider attribute='class' defaultTheme='system' enableSystem disableTransitionOnChange>
          <SessionProvider>
            <UserProvider>
              <SocketProvider>
                {children}
              </SocketProvider>
              <Toaster />
            </UserProvider>
          </SessionProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}